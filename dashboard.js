'use strict';
const F=window.FF;
F.card=p=>`<article class="project-card" data-action="open-project" data-id="${p.id}"><div class="project-thumb"><div class="thumb-artboard"></div></div><div class="project-meta"><div class="project-meta-text"><h3 class="project-name">${F.esc(p.name)}</h3><p class="project-date">Editado ${F.date(p.updated_at)}</p></div><button class="card-menu" data-action="project-menu" data-id="${p.id}">${F.icon('more-vertical',18)}</button></div></article>`;
F.accountSheet=()=>`<div class="sheet-backdrop" data-action="close-panel"></div><section class="sheet"><div class="sheet-handle"></div><div class="sheet-header"><h3>Conta</h3><button class="icon-btn" data-action="close-panel">${F.icon('x')}</button></div><p class="muted">${F.s.guest?'Você está usando armazenamento local.':F.esc(F.s.user?.email||'')}</p><div class="sheet-actions"><button class="secondary-btn" data-action="install-app">${F.icon('download',18)} Instalar</button><button class="danger-btn" data-action="sign-out">${F.icon('log-out',18)} Sair</button></div></section>`;
F.menu=()=>`<div class="sheet-backdrop" data-action="close-menu"></div><div class="context-menu" style="right:16px;top:86px"><button data-action="duplicate-project" data-id="${F.s.menu}">${F.icon('copy',18)} Duplicar</button><button data-action="delete-project" data-id="${F.s.menu}" style="color:#ff8796">${F.icon('trash-2',18)} Excluir</button></div>`;
F.dashboard=()=>{const q=F.s.projects.filter(p=>p.name.toLowerCase().includes(F.s.search.toLowerCase()));const i=(F.s.user?.email?.[0]||'G').toUpperCase();F.app.innerHTML=`<main class="dashboard-view view"><header class="dashboard-header"><div class="brand"><div class="brand-mark">${F.icon('frames',22)}</div><div><h1>FrameFlow</h1><p>${F.s.guest?'Modo convidado':'Sincronizado'}</p></div></div><button class="avatar-btn" data-action="account-menu">${i}</button></header><section class="dashboard-main"><div class="hero"><div><h2>Seus projetos</h2><p>Crie, edite e exporte pelo celular.</p></div><button class="secondary-btn" data-action="new-project">${F.icon('plus',18)} Novo</button></div><label class="searchbar">${F.icon('search',18)}<input id="projectSearch" value="${F.esc(F.s.search)}" placeholder="Pesquisar projetos"></label><div class="project-grid">${q.length?q.map(F.card).join(''):`<div class="empty-state"><div>${F.icon('layout-template',44)}<h3>Nenhum projeto aqui</h3><p>Crie seu primeiro layout.</p></div></div>`}</div></section><button class="fab" data-action="new-project">${F.icon('plus',25)}</button>${F.s.panel==='account'?F.accountSheet():''}${F.s.menu?F.menu():''}</main>`};
