'use strict';
const F=window.FF;
F.layersSheet=()=>`<div class="sheet-backdrop" data-action="close-panel"></div><section class="sheet"><div class="sheet-handle"></div><div class="sheet-header"><h3>Layers</h3><button class="icon-btn" data-action="close-panel">${F.icon('x')}</button></div><div class="layer-list">${[...F.s.doc.layers].sort((a,b)=>(b.z||0)-(a.z||0)).map(l=>`<div class="layer-row ${l.id===F.s.selected?'active':''}" data-action="select-layer-row" data-id="${l.id}"><span>${F.icon({rect:'square',ellipse:'circle',text:'type',image:'image'}[l.type]||'box',17)}</span><span class="layer-row-name">${F.esc(l.name||l.type)}</span><button class="mini-icon" data-action="toggle-visibility" data-id="${l.id}">${F.icon(l.visible===false?'eye-off':'eye',16)}</button><button class="mini-icon" data-action="toggle-lock" data-id="${l.id}">${F.icon(l.locked?'lock':'unlock',16)}</button><button class="mini-icon" data-action="move-layer-up" data-id="${l.id}">${F.icon('arrow-up',16)}</button><button class="mini-icon" data-action="move-layer-down" data-id="${l.id}">${F.icon('arrow-down',16)}</button></div>`).join('')||'<p class="muted">Adicione uma layer.</p>'}</div></section>`;
F.propsSheet=l=>`<div class="sheet-backdrop" data-action="close-panel"></div><section class="sheet"><div class="sheet-handle"></div><div class="sheet-header"><h3>Propriedades</h3><button class="icon-btn" data-action="close-panel">${F.icon('x')}</button></div>${!l?'<p class="muted">Selecione uma layer.</p>':`<div class="property-grid"><div class="field full"><label>Nome</label><input class="field-input" data-prop="name" value="${F.esc(l.name||'')}"></div>${[['x','X'],['y','Y'],['width','Largura'],['height','Altura'],['rotation','Rotação']].map(([p,n])=>`<div class="field"><label>${n}</label><input class="field-input" type="number" data-prop="${p}" value="${Math.round(l[p]||0)}"></div>`).join('')}<div class="field"><label>Opacidade</label><input class="field-input" type="number" min="0" max="100" data-prop="opacityPercent" value="${Math.round((l.opacity??1)*100)}"></div><div class="field full"><label>Cor</label><div class="color-row"><input type="color" data-prop="fill" value="${l.fill||'#7c5cff'}"><input class="field-input" data-prop="fill" value="${l.fill||'#7c5cff'}"></div></div>${['rect','image'].includes(l.type)?`<div class="field full"><label>Arredondamento</label><input class="field-input" type="number" data-prop="radius" value="${l.radius||0}"></div>`:''}${l.type==='text'?`<div class="field full"><label>Texto</label><textarea class="field-input" style="height:86px;padding:10px" data-prop="text">${F.esc(l.text||'')}</textarea></div><div class="field"><label>Tamanho</label><input class="field-input" type="number" data-prop="fontSize" value="${l.fontSize||28}"></div><div class="field"><label>Peso</label><select class="field-input" data-prop="fontWeight">${[400,600,700,800].map(x=>`<option ${l.fontWeight==x?'selected':''}>${x}</option>`).join('')}</select></div>`:''}</div><div class="sheet-actions"><button class="secondary-btn" data-action="duplicate-layer">${F.icon('copy',17)} Duplicar</button><button class="danger-btn" data-action="delete-layer">${F.icon('trash-2',17)} Excluir</button></div>`}</section>`;
F.exportSheet=()=>`<div class="sheet-backdrop" data-action="close-panel"></div><section class="sheet"><div class="sheet-handle"></div><div class="sheet-header"><h3>Exportar</h3><button class="icon-btn" data-action="close-panel">${F.icon('x')}</button></div><p class="muted">Exporte como PNG ou JSON editável.</p><div class="sheet-actions"><button class="primary-btn" data-action="export-png">${F.icon('image-down',18)} PNG</button><button class="secondary-btn" data-action="export-json">${F.icon('file-json',18)} JSON</button></div></section>`;
